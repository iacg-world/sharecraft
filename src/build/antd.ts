import {
  Button, Col, Row, Select, Layout, LayoutContent, LayoutFooter,
  LayoutHeader, Input, InputNumber, Textarea, ButtonGroup, Slider, Menu, MenuItem, Card, Dropdown, Modal,
  Tabs, TabPane, Flex, Pagination, Empty, List, ListItem, Tooltip,
} from 'ant-design-vue'
import { Plugin } from 'vue'

export default [
  Button, Col, Row, Select, Layout, LayoutContent, LayoutFooter,
  LayoutHeader, Input, InputNumber, Textarea, ButtonGroup, Slider, Menu, MenuItem, Card, Dropdown, Modal,
  Tabs, TabPane, Flex, Pagination, Empty, List, ListItem, Tooltip,
] as unknown as Plugin[]