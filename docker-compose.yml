version: "3"
services:
  sharecraft:
    image: registry.cn-hangzhou.aliyuncs.com/supercll/craft-fe:{{tag}}
    container_name: "sharecraft"
    ports:
      - "8080:8080"
    env_file:
      - .env
    networks:
      - sharecraft_net
networks:
  sharecraft_net:
