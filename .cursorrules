# ShareCraft - 零代码建站平台 Cursor Rules

## 项目概述

ShareCraft 是一个基于 Vue 3 的零代码建站平台，支持拖拽式创建活动页/落地页/分享页，并集成了 AI Agent 功能，可通过自然语言自动生成页面组件。

## 技术栈

- 前端框架：Vue 3.5 + TypeScript 5.6
- 状态管理：Vuex 4
- UI 框架：Ant Design Vue 4
- 构建工具：Vite 5
- 包管理：pnpm
- 代码质量：ESLint 9 + Prettier 3
- 样式预处理：SCSS
- AI 集成：OpenAI、Claude

## 核心编码规范

### TypeScript 规范

- 使用 `type` 声明类型，禁止使用 `interface`
- 优先使用具名导出，避免默认导出
- 禁止使用 `any` 和 `as` 绕过类型检查
- 禁止使用 Enum，使用 `const {} as const` 替代
- 使用 `@/` 路径别名引用 src 目录
- API 响应和请求必须定义类型

```typescript
// ✅ 正确
type ComponentProps = {
  id: string
  name: 'c-text' | 'c-image' | 'c-shape'
  props: Partial<AllComponentProps>
}

const ComponentStatus = {
  Active: 'active',
  Inactive: 'inactive',
} as const
type ComponentStatus = (typeof ComponentStatus)[keyof typeof ComponentStatus]

// ❌ 错误
interface ComponentProps {} // 禁止 interface
const data: any = response // 禁止 any
enum Status {} // 禁止 enum
```

### Vue 组件规范

- 使用 `<script setup lang="ts">` 语法
- 组件命名使用 PascalCase
- Props 使用 camelCase，事件使用 kebab-case
- 使用 `withDefaults` 定义默认值
- 优先使用 Composition API

```vue
<script setup lang="ts">
import { ref, computed } from 'vue'
import { useStore } from 'vuex'
import type { GlobalDataProps } from '@/store'

type Props = {
  componentData: ComponentData
  isEditing?: boolean
}

const props = withDefaults(defineProps<Props>(), {
  isEditing: false,
})

const emit = defineEmits<{
  'update:component': [data: ComponentData]
  'delete-component': [id: string]
}>()

const store = useStore<GlobalDataProps>()
</script>
```

### 状态管理规范 (Vuex)

- Store 模块：editor、user、templates、global
- Mutations 使用动词开头的 camelCase
- Actions 处理异步操作和 API 调用
- 使用 `actionWrapper` 封装 API 调用

```typescript
// store 模块示例
type EditorState = {
  components: ComponentData[]
  currentElementId: string
  page: PageData
  isDirty: boolean
}

const mutations = {
  addComponent: setDirtyWrapper((state, component: ComponentData) => {
    state.components.push(component)
  }),
  updateComponent: setDirtyWrapper((state, payload: UpdateComponentData) => {
    // 更新逻辑
  }),
}
```

## 代码风格

### 格式化规则

- 缩进：2 空格
- 引号：单引号
- 分号：不使用
- 尾逗号：ES5 风格（对象/数组最后一项加逗号）
- 对象花括号间距：`{ a: 1 }`
- 数组无间距：`[1, 2, 3]`

### 命名规范

- 文件名：PascalCase（组件）、camelCase（工具/hooks）
- 组件：PascalCase
- 变量/函数：camelCase
- 常量：camelCase 或 UPPER_SNAKE_CASE
- 类型：PascalCase

### 禁止事项

- 禁止使用 `try-catch`（全局错误处理已配置）
- 禁止使用 `var`，使用 `const` / `let`
- 禁止使用 `==`，使用 `===`
- 禁止添加非必要注释
- 禁止使用 console.log（除调试外）

## 项目结构约定

```
src/
├── components/        # 通用组件
│   ├── AI*.vue       # AI 相关组件
│   ├── C*.vue        # 业务渲染组件（CText、CImage）
│   ├── Edit*.vue     # 编辑器相关组件
│   └── *.vue         # 其他通用组件
├── hooks/            # Composition API hooks
│   └── use*.ts       # 自定义 hooks
├── store/            # Vuex 状态管理
│   ├── editor.ts     # 编辑器状态
│   ├── user.ts       # 用户状态
│   ├── templates.ts  # 模板状态
│   └── global.ts     # 全局状态
├── utils/            # 工具函数
│   ├── ai*.ts        # AI 相关工具
│   └── *.ts          # 其他工具
├── views/            # 页面视图
└── types/            # 类型定义
```

## 组件开发

### 核心组件类型

组件类型仅支持：`c-text`、`c-image`、`c-shape`

```typescript
type ComponentData = {
  id: string
  name: 'c-text' | 'c-image' | 'c-shape'
  props: Partial<AllComponentProps>
  layerName?: string
  isHidden?: boolean
  isLocked?: boolean
}
```

### 组件属性规范

- 所有位置和尺寸值必须包含 `px` 单位
- 组件 ID 使用 UUID v4 格式
- position 属性默认设为 `absolute`
- 宽度不能超过画布宽度（使用 `limitComponentWidth`）

### 使用 hooks

```typescript
// 使用通用组件 hook
import useComponentCommon from '@/hooks/useComponentCommon'
import { textStylePropNames } from '@/defaultProps'

const { styleProps, handleClick } = useComponentCommon(props, textStylePropNames)
```

## AI 功能开发

### AI API 调用

```typescript
import { callAI } from '@/utils/aiApi'
import { generatePageSchema } from '@/utils/aiSchemaGenerator'

// 调用 AI 生成组件
const result = await generatePageSchema(userInput)
if (result.components) {
  result.components.forEach((component) => {
    store.commit('addComponent', component)
  })
}
```

### AI 配置

- 支持 OpenAI 和 Claude
- 用户配置存储在 localStorage
- 环境变量配置作为后备

```bash
VUE_APP_AI_PROVIDER=openai
VUE_APP_AI_API_KEY=your-api-key
VUE_APP_AI_BASE_URL=https://api.openai.com/v1
VUE_APP_AI_MODEL=gpt-3.5-turbo
```

## 样式规范

### SCSS 使用

- 使用 `<style lang="scss" scoped>`
- 全局样式在 `src/assets/reset-style.scss`
- 使用嵌套但避免超过 3 层
- 响应式断点：768px（移动端）

```scss
.component-container {
  display: flex;
  flex-direction: column;

  .header {
    padding: 16px;
    border-bottom: 1px solid #f0f0f0;
  }

  @media (max-width: 768px) {
    .header {
      padding: 12px;
    }
  }
}
```

## 热键与交互

- 热键管理：hotkeys-js
- 右键菜单：`useCreateContextMenu` hook
- 长按事件：`v-longpress` 指令
- 拖拽功能：vuedraggable

## 开发命令

```bash
pnpm dev          # 启动开发服务器 (端口 8080)
pnpm build        # 构建生产版本
pnpm lint         # 代码检查
pnpm lint:fix     # 自动修复
pnpm format       # 代码格式化
pnpm code:fix     # lint + format
```

## 最佳实践

1. **组件开发**：使用 `useComponentCommon` hook 处理通用逻辑
2. **属性变更**：使用防抖处理，避免频繁更新
3. **图片处理**：统一使用 `ImageProcesser` 组件
4. **撤销/重做**：编辑器状态变更自动记录历史
5. **错误处理**：使用全局 `errorHandler.ts`
6. **API 调用**：使用 `axios.ts` 统一配置

## 注意事项

1. 拖拽功能需处理性能和边界情况
2. 组件宽度不能超过画布宽度（375px 基准）
3. AI 生成的组件必须符合 `ComponentData` 接口
4. 保存前检查 `isDirty` 状态
5. 跨浏览器兼容性测试
6. 移动端触摸交互适配
